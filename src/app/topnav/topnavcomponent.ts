import { Component, Input, Output, EventEmitter } from '@angular/core';

@Component({
    selector: 'nav',
    templateUrl: 'topnavtemplate.html',
})
export class TopNav {
    ngOnInit() {
        
    }

}